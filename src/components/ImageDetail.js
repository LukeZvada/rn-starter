import React from "react";
import { Text, StyleSheet, View, Button, TouchableOpacity } from "react-native";

const ImageDetial = () => {
    return (
        <Text>Show img of forest</Text>
    )
};

const styles = StyleSheet.create({});

export default ImageDetial;